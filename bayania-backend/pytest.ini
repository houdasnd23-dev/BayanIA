[pytest]
pythonpath = .
asyncio_mode = auto

markers =
    unit: tests unitaires, sans base de données réelle
    integration: tests d'intégration, avec base SQLite en mémoire (isolée, recréée à chaque test)